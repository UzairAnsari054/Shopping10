interface Product {
    id: string
    name: string
    imageUrl: string
    originalPrice: number
    originalPrice: number
    offerPercentage: number
    rating: number
    ratingCount: number
    tags: string[]
}